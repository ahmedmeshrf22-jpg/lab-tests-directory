name: Build Tahalil Alakkad V80 REAL LAB SYSTEM

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'Project3_AK_V80_REAL_LAB_SYSTEM.zip'
      - '.github/workflows/build-apk.yml'

permissions:
  contents: read

jobs:
  build-release:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.14.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.14.1'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        shell: bash
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          sdkmanager 'platform-tools' 'platforms;android-36' 'build-tools;35.0.0'

      - name: Extract V80 project
        shell: bash
        run: |
          set -euo pipefail
          ZIP_FILE="$GITHUB_WORKSPACE/Project3_AK_V80_REAL_LAB_SYSTEM.zip"
          test -f "$ZIP_FILE"

          rm -rf "$GITHUB_WORKSPACE/android_project"
          mkdir -p "$GITHUB_WORKSPACE/android_project"
          unzip -q -o "$ZIP_FILE" -d "$GITHUB_WORKSPACE/android_project"

          SETTINGS_FILE="$(find "$GITHUB_WORKSPACE/android_project" -type f -name 'settings.gradle.kts' -print -quit)"
          test -n "$SETTINGS_FILE"
          PROJECT_DIR="$(dirname "$SETTINGS_FILE")"
          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"
          echo "Project: $PROJECT_DIR"

      - name: V80 real lab system audit
        shell: bash
        run: |
          set -euo pipefail
          SCREEN="$PROJECT_DIR/app/src/main/java/com/example/ui/LabTestsScreen.kt"
          WORKLIST="$PROJECT_DIR/app/src/main/java/com/example/ui/DailyWorklistScreen.kt"
          NAV="$PROJECT_DIR/app/src/main/java/com/example/ui/DailyServicesNav.kt"
          VM="$PROJECT_DIR/app/src/main/java/com/example/ui/LabTestsViewModel.kt"
          GUIDE="$PROJECT_DIR/app/src/main/java/com/example/ui/TestGuideDialog.kt"
          SETTINGS="$PROJECT_DIR/app/src/main/java/com/example/ui/ManagerSettingsDialog.kt"
          GENERATOR="$PROJECT_DIR/app/src/main/java/com/example/util/PdfGenerator.kt"
          MANIFEST="$PROJECT_DIR/app/src/main/AndroidManifest.xml"

          test -f "$SCREEN" && test -f "$WORKLIST" && test -f "$NAV" && test -f "$VM"
          test -f "$GUIDE" && test -f "$SETTINGS" && test -f "$GENERATOR" && test -f "$MANIFEST"
          test -f "$PROJECT_DIR/V80_REAL_LAB_SYSTEM_NOTES.txt"

          grep -Fq 'versionCode = 80' "$PROJECT_DIR/app/build.gradle.kts"
          grep -Fq 'versionName = "5.0-real-lab-system-v80"' "$PROJECT_DIR/app/build.gradle.kts"

          # Real operational dashboard + daily worklist.
          grep -Fq 'fun DailyWorklistScreen' "$WORKLIST"
          grep -Fq 'تشغيل اليوم' "$WORKLIST"
          grep -Fq 'المرحلة التالية' "$WORKLIST"
          grep -Fq 'viewModel.dailyOrders.collectAsState()' "$WORKLIST"
          grep -Fq 'fun loadDailyOrders' "$VM"
          grep -Fq 'fun findCustomerById' "$VM"
          grep -Fq 'whereGreaterThanOrEqualTo("created_at_ms", startToday)' "$VM"
          grep -Fq '"worklist"' "$SCREEN"
          grep -Fq 'تشغيل المعمل' "$SCREEN"
          grep -Fq 'طلبات اليوم' "$SCREEN"
          grep -Fq 'بحث وأسعار' "$SCREEN"
          grep -Fq 'العملاء والطلبات' "$SCREEN"
          grep -Fq 'onWorklist' "$NAV"

          # Stable production features retained from V78/V74.
          grep -Fq 'Mini3DSwipeServiceCard' "$NAV"
          grep -Fq 'R.drawable.staff_new_order_3d' "$SCREEN"
          grep -Fq 'R.drawable.mgr_reports_3d' "$SCREEN"
          grep -Fq 'fun generateQuickTestsImage' "$GENERATOR"
          grep -Fq 'generateGuideHologramImage' "$GUIDE"
          grep -Fq 'fun changeAdminPin' "$VM"
          grep -Fq 'تغيير PIN الإدارة' "$SETTINGS"
          grep -Fq 'adminPinFailedAttempts >= 5' "$VM"
          grep -Fq 'android:allowBackup="false"' "$MANIFEST"
          grep -Fq 'android:usesCleartextTraffic="false"' "$MANIFEST"

          echo 'V80 real lab system audit passed'

      - name: Restore original release signing
        shell: bash
        env:
          SIGNING_BUNDLE_B64: ${{ secrets.TAHALIL_SIGNING_BUNDLE_B64 }}
        run: |
          set -euo pipefail
          if [ -z "${SIGNING_BUNDLE_B64:-}" ]; then
            echo 'ERROR: Secret TAHALIL_SIGNING_BUNDLE_B64 is missing'
            exit 1
          fi

          SIGN_DIR="$RUNNER_TEMP/tahalil-signing"
          rm -rf "$SIGN_DIR"
          mkdir -p "$SIGN_DIR"

          printf '%s' "$SIGNING_BUNDLE_B64" | base64 -d > "$SIGN_DIR/signing.zip"
          unzip -q -o "$SIGN_DIR/signing.zip" -d "$SIGN_DIR"

          test -f "$SIGN_DIR/tahalil-alakkad-release.jks"
          test -f "$SIGN_DIR/signing.env"

          set -a
          source "$SIGN_DIR/signing.env"
          set +a

          KEYSTORE_PATH="$SIGN_DIR/tahalil-alakkad-release.jks"
          keytool -list \
            -keystore "$KEYSTORE_PATH" \
            -storepass "$STORE_PASSWORD" \
            -alias "$KEY_ALIAS" >/dev/null

          echo "KEYSTORE_PATH=$KEYSTORE_PATH" >> "$GITHUB_ENV"
          echo "STORE_PASSWORD=$STORE_PASSWORD" >> "$GITHUB_ENV"
          echo "KEY_ALIAS=$KEY_ALIAS" >> "$GITHUB_ENV"
          echo "KEY_PASSWORD=$KEY_PASSWORD" >> "$GITHUB_ENV"

      - name: Build signed V80 Release APK
        shell: bash
        run: |
          set -euo pipefail
          cd "$PROJECT_DIR"

          gradle \
            --no-daemon \
            --no-configuration-cache \
            --no-parallel \
            --max-workers=2 \
            --console=plain \
            :app:assembleRelease

          APK_FILE="$(find app/build/outputs/apk/release -type f -name '*.apk' -print -quit)"
          test -f "$APK_FILE"

          mkdir -p "$GITHUB_WORKSPACE/output"
          cp "$APK_FILE" "$GITHUB_WORKSPACE/output/Tahalil-Alakkad-V80-REAL-LAB.apk"
          ls -lh "$GITHUB_WORKSPACE/output/Tahalil-Alakkad-V80-REAL-LAB.apk"

      - name: Verify APK signature
        shell: bash
        run: |
          set -euo pipefail
          APK="$GITHUB_WORKSPACE/output/Tahalil-Alakkad-V80-REAL-LAB.apk"
          APKSIGNER="$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -1)"
          test -x "$APKSIGNER"
          "$APKSIGNER" verify --verbose --print-certs "$APK"

      - name: Upload V80 APK
        uses: actions/upload-artifact@v4
        with:
          name: Tahalil-Alakkad-V80-REAL-LAB
          path: output/Tahalil-Alakkad-V80-REAL-LAB.apk
          if-no-files-found: error
          retention-days: 30
          
